{{>partials/header}}

<div class="container-fluid max-width-1200 mx-auto my-2">
    {{>partials/messages}}
</div>

<div class="d-flex align-items-center justify-content-center container max-width-1200 mx-auto min-height-60">
    <div class="login container p-5 mt-5 rounded bg-light shadow">
        <h1 class="text-center text-primary mb-4"> Login </h1>
        <form method="POST" action="/login" class="form-floating">
            <div class="form-floating row">
                <input class="form-control" type="email" id="email" name="email" placeholder="Digite seu e-mail aqui"/>
                <label for="email">E-mail</label>
            </div>
            <div class="form-floating row my-3">
                <input class="form-control" type="password" id="password" name="password" placeholder="Digite sua senha aqui"/>
                <label for="password">Senha</label>
            </div>


            <button class="btn btn-outline-primary my-3">Entrar</button>

            <br/>
            <small class="text-center text-muted">Não tem uma conta ainda? Então <a href="/register" class="link-primary">cadastre-se<a> agora.</small>
        </form>
    </div>
</div>

{{>partials/footer}}